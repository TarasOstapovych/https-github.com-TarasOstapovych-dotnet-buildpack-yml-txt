#!/usr/bin/env bash
# bin/detect <build-dir>
file="../base_path.yml.txt"
name=$(cat "$file")
files="../ignore_path.yml.txt"
ignore=$(cat "files")

if [ -n "$(find $name $1 -iname Startup.cs -not -path "$ignore")" ]; then
  echo "ASP.NET Core" && exit 0
else
  echo "no" && exit 1
fi
